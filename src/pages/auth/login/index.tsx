import { LoginForm } from "@/page-components/auth/login/LoginForm";
import React, { FC } from "react";

const Login: FC = () => (
  <div className="container py-10">
    <LoginForm />
  </div>
);

export default Login;
